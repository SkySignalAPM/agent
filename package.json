{
  "name": "skysignal-agent-tests",
  "private": true,
  "description": "Test suite for skysignal:agent Meteor package",
  "scripts": {
    "test": "mocha --require tests/setup.js --recursive tests/unit/ --timeout 10000",
    "test:watch": "mocha --require tests/setup.js --recursive tests/unit/ --timeout 10000 --watch"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-env": "^7.26.0",
    "@babel/register": "^7.25.0",
    "babel-plugin-module-resolver": "^5.0.2",
    "chai": "^4.5.0",
    "mocha": "^10.8.0",
    "sinon": "^19.0.0",
    "sinon-chai": "^3.7.0"
  }
}
